# 「声は壁を透して」制作引き継ぎメモ v16
更新：2026年6月10日 / 対象ブランチ：`main`

作業再開時はこのファイルを最初に読むこと。旧版（v5〜v15）は `docs/archive/` に移動済み。

---

## 0. 最初に読むもの（新しいファイル配置）

1. `引き継ぎメモ_v16.md`（このファイル・ルート唯一の引き継ぎメモ）
2. `docs/改善計画_2026-06-10.md` ── **今日の全作業の詳細記録と残タスクはここ**
3. `docs/設計書 確定版.md`
4. `docs/アプリ基盤設計書_v1.md`
5. `docs/画像仕様書_v1.md`（`puzzle_recv.jpg` 方式は廃止済み）
6. `docs/課題メモ_2026-06-05.md`（残項目は本メモ §6 の表が最新）

過去のテストプレイFB・AI改善案・旧メモは `docs/archive/`。

---

## 1. プロジェクト概要と構成

- 松川事件モチーフの架空冤罪事件「M川事件」を追うARG。静的HTML/JS、ビルド不要。GitHub Pages 公開（公開リポジトリ＝ドキュメントも公開される点に注意）。
- 入口 `index.html`（深沢のブログ記事）→ 本体 `phone_shell.html`（スマホUI SPA）。
- ページ本文 `pages/*.js`、解析ツール `tools/*.js`（morse / hexconv の2つのみ。**新規ツールは作らない**）、進行ロジック `os.js`（v3.2）。

## 2. 検証ワークフロー（本文・キーワードを変えたら毎回）

```powershell
node --check os.js            # 編集したJSすべてに
node extract_scenario.js      # scenario_text.txt 再生成
node check_progression.js     # 仮想プレイヤーの通しプレイ検証
```

`check_progression.js` は読み取り専用リンタ。完走可否・キーワード衝突・同時解放・
一括連鎖・答えの本文露出を機械検査する。D/E の「(情報)」項目は仕様（前提不足応答あり）。

## 3. 設定上の不変条件（変更禁止・最重要）

- **メタ構造**：プレイヤーは探索開始時に「壁」を一枚透過して架空の側に来ている。
  追記しない＝切断されGoogle「松川事件」へ送還／追記する＝補記で実在への橋。
  **この設定は絶対に言語化しない。演出のみで示す。**
- **「世界線」という語は使用禁止**（制作者指定）。語彙は「壁のこちら側／そちら側」。
- 不明送信元（繧ｿ◆縺薙％??）＝タコ内の機械。**正体は最後まで明かさない**。小蘭のリアルタイム送信に見せない。
- 猫塚ふみは存在しない（小蘭に統合済み）。内部ID `fumi_tegami`・キー `koe_fumi_note` は実装名として残置。
- 呼称ルール：小蘭が語るとき「清治さん」／記録上の固有名詞のみ「猫塚清治」。
- 可視文言に「架空」を使わない（免責表示ブロックは例外）。
- hidden の鍵「あなたはここにいた」は fumi_tegami がほぼ初出（photo_001 は「この人はここにいた」）。

## 4. 進行仕様の要点（os.js v3.2）

- prereq連鎖は「**閲覧済み**」基準（`koe_viewed`）。1ページ読むごとに次の1ページが開く。
- `submitKeyword` は完全一致。`prereqBlocked`（一致したが前提不足・キーワード未消費）と
  `missStreak`（連続失敗。3回で汎用、6回以上でスポーク名指しのヒント）を返す。
- wiki_add / wiki_skip / epilogue は `manual:true`＝choice内ボタン（bNavigate→markRestored）で解放。
- メッセージ未読は導出値（`koe_msgs_read`。pushNotif はバッジを直接増やさない）。
- **keywords 配列は自由編集してよい**（2026-06-10 制作者許可）。ただし変更後の検証必須。
- 解放キーワード一覧と謎：kiroku_002＝黒塗り復元「クラウド」／bbs_003＝名簿照合「蛸川小蘭」／
  map_002・003＝黒塗り地名推理／telegram_002＝ツール解読「0816」（素材は telegram_001 の
  接触記録ログ。**障子モールスは廃止 2026-06-10**。第1段ヘッダ 16 10＝タコ）／
  data_trace＝黒塗り復元「バックアップ」（素材は tegami の添え状断片）／hidden＝モールス→16進。
- 第2層（os.js v3.3）：背骨＝inochi →閲覧→ loop →検索「バックアップ」→ data_trace →閲覧→ receiver_lock。
  資料棚（voices/tegami/sns/momo）は hub_002 閲覧で一括解放・任意閲覧（進行条件にしない）。
- セクタマップ（os.js v3.4・ホーム4つ目のアプリ🐙）：タコ躯体図SVGで進捗可視化。
  8本足＝5スポーク＋深層記録＋資料棚＋最深部（計29吸盤）。閲覧で点灯・未読は明滅・
  追記EDクリアで全点灯。詳細は改善計画の「セクタマップ」節。

## 5. 第2層の「背骨化＋中間に謎を1つ」──【実装済み 2026-06-10】

§6 の表先頭にあった承認済みタスク。実装詳細は `docs/改善計画_2026-06-10.md` の
「第2層 背骨化＋中間謎」の節を参照。要点：

- 背骨：inochi →閲覧→ loop →検索→ data_trace（os.js v3.3）。
- 資料棚：voices/tegami/sns/momo は hub_002 閲覧で一括解放・任意閲覧。
- 中間謎：tegami の「断片　添え状の控え」の赤い黒塗り＝「バックアップ」を検索で data_trace 解放。
- 注意：`_loadPage` は遷移先を無条件 markRestored するため、ページ内ナビボタンで
  ロック中ページへ直接 bNavigate しないこと（momo→loop は hub_002 行きに差し替え済み）。
- 検証済み：完走100%／衝突なし／答えの本文露出なし。F検査の
  「(情報) hub_002 閲覧で資料棚を一括解放（仕様）」は意図どおり。

## 6. バックログ（優先順）

| 項目 | 状態 |
|---|---|
| 第2層 背骨化＋中間謎（§5） | **実装済み（2026-06-10）**。実ブラウザでの「バックアップ」謎の体感確認のみ残 |
| **index.html の「充電端子」→LANケーブルに差し替え** | **実装済み（2026-06-10）**。「LANケーブルの差し込み口によく似ている／どの規格とも違う」の描写に書き換え。給電と接続は「給電の口と、どこかへ繋ぐ口を、ひとつで兼ねているらしい」の一文で整理（data_trace「端子を繋げばタコは満たされた」と整合）。課題メモの該当項目も解消済みに更新 |
| **要作成画像の生成プロンプト作成（別AI生成用）** | **作成済み（2026-06-10）→ `docs/画像生成プロンプト_v1.md`**。①障子0816 は**同日廃止**（Eスポークを接触記録ログのHTML描画に再設計したため生成不要）。残る対象：②家の写真+「M川事件」 ③タコLAN端子版 ④-a 駅名標版 ④-b 新聞日付版（日英プロンプト＋生成後チェックリスト。日本語文字は後合成推奨）。**画像の生成と差し替えは制作者の作業として残**。④は画像観察化の実装とセットで |
| 実ブラウザ通しテスト | 未。重点：クラウド謎・バックアップ謎（tegami断片→data_trace）・接触記録謎（0816）が解けるか／資料棚の見え方／**セクタマップ（🐙）のSVG描画・足ラベル位置・明滅・クリア時の全点灯**／切断・追記両EDの演出／固定率・未読ドット／hexconv自動区切り |
| CREDITS.md（画像出典管理） | リーガルレビュー対応（2026-06-11）で新設。CC BY-SA の photo_mkawa_now 以外は全て AI生成（自作）と制作者確認済み・記入済み。**残：photo_mkawa_now を改変していれば photo_002 のゲーム内クレジットに「（改変あり）」を追記**。以後、画像追加時は CREDITS.md に記入 |
| photo_002/003 の画像観察化（謎の答えを画像内へ） | 画像再生成が必要。`docs/画像仕様書_v1.md` と調整（上記プロンプト作成タスク④と連動） |
| 沼の中に遊園地がある問題 | 未対応（設定） |
| 押し入れにいる必要がない問題 | 未対応（軽微。手記①に一文足す案あり） |
| 「福島市」実在語で進みすぎる問題 | 未対応（要再現確認） |
| アドレスバーUX | 入力欄focus時に全選択は実装済み。体感は要ブラウザ確認 |

解消済み（2026-06-10）：座標フレーバー／北へ行け／月湯温泉重複／封筒・書けない手紙／
問いた→問うた／bbs_001出せない手紙矛盾／未読バッジ復活／ハッシュタグ風タグ／
「架空」文言／紙の質の宙吊り／16進スペース入力／変換表のボタン誤認／追記EDの演出格差。

## 7. 作業スタイル（制作者の希望）

- 実装と**データの構造把握程度の調査**は、トークン節約になる限り常に **Sonnet / Haiku の
  サブエージェント**に委譲し、親（メインの Claude）はシナリオ・設計・文言指定・検証・
  品質チェックを担当する（2026-06-10 制作者指示・常時適用）。
- サブエージェントには：対象ファイル限定／編集前に必読／extract・check は親が実行／
  「世界線」禁止と上記不変条件、を毎回プロンプトで明示すること。
- エージェントの創作部分は親が必ず目視レビュー（過去に既存の良い記述を上書きした事故あり。
  bbs_002 名簿の例 → `docs/改善計画_2026-06-10.md` 参照）。

## 8. 直近のコミット

- `53e54da` Overhaul pacing, puzzles, endings per 改善計画 2026-06-10
- それ以降の未コミット分（テストプレイFB第2弾対応・ドキュメント整理）は本メモと同時にコミットされる。
