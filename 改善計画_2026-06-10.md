# 改善計画 2026-06-10 ── 「面白くない」の根本対策

対象：進行バグ／キーワード詰み／謎解きの質／シナリオ構造。
検証ツール：`check_progression.js`（新規・読み取り専用。os.js の実ロジックを Node 上で動かし、仮想プレイヤーに通しプレイさせる）。
実行：`node check_progression.js`。本文・キーワードを変更したら extract_scenario.js と併せて毎回実行すること。

---

## 1. 進行検証の結果（機械的に確定した事実）

完走は可能。全35ページ到達、詰みなし。キーワードは謎解き答え2件を除き、すべて到達時点の可視テキストに存在する。その上で以下が確定。

### 1-1. 「月湯温泉」1検索で手記①②が同時解放【バグ】
kiroku_001 と kiroku_002 が同じキーワードを持つため、`submitKeyword` のループ内で
kiroku_001 が解放された直後に kiroku_002 の prereq が満たされ、**1回の検索で両方開く**。
手記①を読む前に②が開いており、読書順が壊れる。
→ 対処：kiroku_002 のキーワードを変更（例：手記①末尾の固有語）するか、kiroku_002 を prereq 自動解放に変更。
※ os.js の keywords 配列は手動管理のため、変更値の決定は制作者。

### 1-2. 第2層が7ページ一括解放【ペーシング崩壊】
`checkPrereqUnlocks` は「prereq が**解放済み**」で連鎖するため、inochi 解放後の最初の
ページ表示で voices → tegami → sns → momo → loop → data_trace → **receiver_lock** まで
全部いっぺんに開く。hub_002 の本文は「一つ読めば次が自動で開く」と宣言しているが、
実際は一括投下で、通知も束で届く。第3層の入口（receiver_lock）まで先に見えてしまう。
→ 対処：連鎖条件を「prereq が**閲覧済み**（koe_history に存在）」に変更。
`_loadPage` は既に `addHistory` を呼んでいるので、os.js 側の条件を1箇所変えれば成立する。

### 1-3. エンディングが選択前に全解放【演出崩壊】
hidden 解放の連鎖で fumi_tegami / choice / **wiki_add / wiki_skip / epilogue** が同時に開く。
「追記しますか」の選択を提示する前に、「追記しない選択」「エピローグ（GAME CLEAR）」が
解放済みとしてファイル一覧・通知に並ぶ。選択の重みが消える。
→ 対処：wiki_add / wiki_skip / epilogue は prereq 連鎖から外し、choice ページ内の
ボタン押下で `markRestored` する（選択そのものを解放トリガーにする）。

### 1-4. 最終謎の答えが序盤に露出【謎解き無効化】
hidden の解錠キーワード「あなたはここにいた」が **photo_001 の裏面メモ**（スポークB最初の
ページ）にそのまま書かれている。最終モールス解読の答えを序盤から知っている状態になる上、
序盤に検索すると prereq 未達で**完全無反応**＝バグにしか見えない。
→ 対処：photo_001 の裏面メモを設計書どおり「**この人はここにいた**」へ変更。
モチーフの反復は保ちつつ、最終解読で「あの写真の言葉が"あなた"に変わって返ってくる」
という着地に強化される。fumi_tegami（hidden 後）での全文一致は問題なし。

### 1-5. 「正しいが今は開かない」キーワードが完全無反応【詰み体感の主因】
prereq 未達のキーワードを検索しても、ハズレ入力と同じ無反応。検証で10件確認
（月湯温泉/猫塚清治/清治/昭和24年/M川駅/蒼沼ブルーランド 等は prereq 未達時点で本文に
出現済み）。プレイヤーには「キーワードが間違っている」と区別できない。
→ 対処：`submitKeyword` の戻り値に「一致したが前提不足」の状態を追加し、UI で
「── 照合一致。ただし参照元の記録が不足しています」のように返す。完全一致仕様は維持。

### 1-6. その他
- `pages/soran_profile.js` は PAGES 未登録のままロードだけされている（統合済みの遺物）。削除推奨。
- `pages/soran_intro.js` も PAGES 外（メッセージスレッドとして動作中なので現状OK、登録不要の旨をコメントに残す）。

---

## 2. 詰まりを演出に変える（ヒントシステム）

「不明送信元＝タコの中の機械」はヒント窓口として完璧な口実。実装は os.js に
失敗カウンタ（解放なし検索の連続回数）を持たせ、閾値で STORY_MSGS に
ナッジを差し込むだけでよい。

- 3回連続ヒットなし → 「…照合失敗。現在の記録を読み直すことを推奨します」
- 6回連続 → 現在地に応じた具体ナッジ（未完了スポークの名指し。例「地図の黒塗りが未照合です」）
- 1-5 の「前提不足」応答と併せて、無反応という状態を消す。

---

## 3. 謎解き再設計（スポーク別）

現状の評価：**D（黒塗り地名の推理）と E（モールス＋16進）だけが謎解きで、
A・B・C は「直前のページの単語を打ち直すだけ」**。テスター高評価も D/E と
感情が動くページ（kiroku_004, bbs_001）に集中している。方針は
「全ロックを ⓐ摩擦ゼロの導線 か ⓑ媒体に根ざした謎解き のどちらかに倒す」。

| ロック | 現状 | 提案 |
|---|---|---|
| kiroku_001（月湯温泉） | archive_aboutに明記 | ⓐのまま（最初の検索成功体験として機能） |
| kiroku_002 | ①と同じ語＝バグ | ⓑ **黒塗り復元パズルの初出**：手記①の赤い黒塗り（現代語）を文脈から推定して検索（例：██████＝「クラウド」）。手記スポークの個性＝「検閲された現代語を読む」になる |
| kiroku_003（猫塚清治） | ②末尾に明記 | ⓐのまま |
| kiroku_004（封筒） | ③末尾に明記 | ⓐのまま。スポーク内は「1謎＋導線」で十分 |
| photo_002（昭和24年） | ①本文に明記 | ⓑ **画像観察**：写真の中の情報（新聞の日付・駅名標）から拾わせる。キャプションから消す |
| photo_003（M川駅） | ②本文に明記 | ⓑ 同上（photo_003 右下「M川事件」→bbs_001 は既に良い設計。同型を増やす） |
| bbs_002（守る会） | ①本文に明記 | ⓐのまま |
| bbs_003（冤罪） | ②本文に明記 | ⓑ **名簿照合**：ビラの賛同者名簿「蛸川 ██」の伏字を他ページと突き合わせて確定させる等、「読む」を「照合する」に変える |
| map_002 / map_003 | 黒塗り推理 | 現状維持（このゲームで一番良い謎） |
| telegram_001/002 | ツール解読 | 現状維持。ただし freesoft への導線（「符号の名前で検索」）はやや暗黙なので 2 のナッジ対象に |
| receiver_lock → hidden | モールス→16進 | 現状維持＋1-4の答え露出修正で価値が回復する |

ポイント：新ツールは増やさない（v15方針どおり）。「黒塗りの復元」を A スポークの
反復メカニクスに昇格させると、検閲・記録抹消というテーマとゲームの動詞が一致する。

---

## 4. シナリオ根本診断

通読した上での結論：**文章は書けているが、構造が「museum tour」になっている。**
枠物語（index.html の拾得記事）と最終盤（fumi_tegami 以降）は強い。問題は中間の25ページ。

### 4-1. 最大の問題：archive_about が全部の謎を最初に答えてしまう
1ページ目で「私は現代から来た／1949-08-16 の夜に清治に救われた／月湯で24年／
清治は記録にない／記録してほしい」まで全て明かす。以降の25ページは**既知の事実の
詳細化**であり、プレイヤーが追う問いが存在しない。読む動機が「次のキーワードを
探すため」しかなくなり、それが「作業感」の正体。

→ **再構成案：archive_about を「問いの提示」に書き換える。**
明かすのは「戸籍のない記録者がいた／このアーカイブを残した／封鎖されている」まで。
以下を中盤の発見に移す：
- 「小蘭は現代人だった」→ 手記の黒塗り現代語・「クラウド」・月湯の既視感から
  **プレイヤーが自力で気づく**（中盤のアハ体験その1。3-の黒塗り復元パズルと連動）
- 「清治が記録から消えている」→ bbs/photo の照合で確定する事実に
- 「記録してください」の依頼 → choice まで温存（現状 choice の依頼が初出の方が刺さる）

### 4-2. 受信者の正体バラし
index.html の追記が「これは私が読むためのものではない。あなたのものだ」と
**hidden の核心をそのまま予告**している。ここは「受信者欄が読めない」で止め、
深沢にも分からないままにする。

### 4-3. 現在時制の脅威・変化がない
全事件が50年前に完結しており、プレイヤーの行為で動くものが最後の wiki 編集まで無い。
不明送信元のメッセージ4通も進捗通知にとどまる。
→ 安価で効く追加：**タコ（機械）の劣化を時間圧にする**。メッセージに
「記録セクタの破損が進行しています」等を混ぜ、進むほど文字化けが増える／
読み終えたページが一瞬ノイズ混じりで再表示される等。「あなたが読むことが
記録の救出である」というコアコンセプトをゲーム内の手応えに変換する。

### 4-4. 中盤に疑念→反転がない
全ページが小蘭の主張をそのまま裏書きする一本道。
→ 反転候補（低コスト）：**「清治は実在したのか」をプレイヤーに疑わせる**。
材料は既にある——写真の出所は老婆の伝聞、名簿の蛸川██は身元不明、清治の記録は
一切ない、語り手は戸籍のない自称時間漂流者。第2層で「全ての証拠が小蘭一人に
遡る」ことを一度突きつけ（例：inochi の「目撃者不在」を自分への疑いに反転させる）、
hidden〜fumi_tegami で確信に変える。そうすると最後の wiki 追記が
「信じることを選ぶ」行為になり、choice の重みが跳ね上がる。

### 4-5. 反復の刈り込み
「あなたはここにいた」「清治さんの名前はそこになかった」「記録から消えた」が
15ページ近くで再説明される。各フレーズの使用箇所を1〜2箇所の最良の位置に絞る
（特に「あなたはここにいた」は 1-4 対応後、fumi_tegami をほぼ初出にする）。
第2層の inochi / voices / sns は論説調が続くため、sns は momo と統合候補。

---

## 5. 推奨着手順

1. **機械修正（1-1〜1-6）**：os.js のロジック2箇所＋photo_001 の1行＋choice の解放方式。
   キーワード変更（kiroku_002）だけ制作者の決定が必要。
2. **応答とヒント（2）**：無反応の根絶。ここまでで「バグ・詰み」系の不満は消える。
3. **謎解き再設計（3）**：A スポークの黒塗り復元、B の画像観察化、C の名簿照合。
4. **シナリオ再構成（4）**：archive_about の書き直し → index 追記の修正 → 疑念パートの
   挿入 → 反復刈り込み。4-1 が最優先（1ページの書き換えで全ページの読む動機が変わる）。
5. 変更のたびに `node extract_scenario.js` と `node check_progression.js` を実行。

---

## 決定が必要な項目（制作者判断）

- [x] kiroku_002 の新キーワード → **黒塗り復元パズル化で実装済み（2026-06-10。キーワード「クラウド」。
      ユーザーが keywords 配列の自由編集を許可）**
- [x] 第2層の解放を「閲覧済み」基準に変える方針の承認（os.js ロジック変更）→ **実装済み（2026-06-10）**
- [x] wiki_add / wiki_skip / epilogue を choice のボタン解放に変える方針の承認 → **実装済み（2026-06-10）**
- [x] photo_001 裏面メモ「この人はここにいた」への変更 → **実装済み（2026-06-10）**
- [x] 4-1 の archive_about 再構成 → **「計画どおり全部隠す」で実装済み（2026-06-10）**
- [x] 4-4 の「清治実在への疑念」反転 → **「入れる」で実装済み（2026-06-10）**

---

## 実装記録（2026-06-10　手順1〜2 完了）

実装済み：

- **1-2** `os.js` v3.2：prereq 連鎖を「閲覧済み」基準に変更。閲覧フラグは履歴とは別の
  `koe_viewed`（localStorage）に保持し、`addHistory` 内で立てる（履歴クリア・50件上限の影響なし）。
  検証：inochi を読むまで voices は開かず、読むと voices のみ開く。第2層フラッド解消。
- **1-3** wiki_add / wiki_skip / epilogue に `manual:true` を付与し連鎖から除外。
  choice 内ボタン（bNavigate → `_loadPage` の markRestored）で解放。
  `_loadPage` は未ロードのページスクリプトを描画前に読み込むよう修正（placeholder 表示の防止）。
- **1-4** photo_001 裏面メモ「あなたはここにいた」→「この人はここにいた」。
  check_progression の E から hidden 答えの露出が消えたことを確認。
- **1-5** `submitKeyword` に `prereqBlocked`（一致したが前提不足。キーワード未消費）を追加。
  ブラウザ検索 UI で「── 照合一致 ──　ただし参照元の記録が不足しています」を表示。
- **1-6** `pages/soran_profile.js` 削除（ALWAYS_LOAD からも除去）。soran_intro.js に
  「PAGES 登録不要（MSG_THREADS として動作）」のコメントを追記。
- **2** ヒントシステム：`os.js` に失敗カウンタ `koe_miss_streak`（解放なし検索の連続回数。
  成功でリセット、前提不足はカウントしない）。UI 側 `_missHint()` が
  3回連続で汎用ナッジ、6回以上で未完了スポークの名指しナッジ（6,9,12…回目は
  不明送信元のトースト通知も発火）。E スポークのナッジは freesoft への導線
  「符号の名前（モールス）で検索」を明示。
- `check_progression.js` を v2 に更新：仮想プレイヤーが1枚ずつ「読む」動作で
  閲覧連鎖を模倣、choice のボタン解放は MANUAL_NAV で模倣。完走100%・F解消を確認。
- `KoeOS.resetAll` に `koe_viewed` / `koe_miss_streak` / `koe_msgs_delivered` /
  `koe_fumi_note` を追加（リセット後の再プレイで通知・選択が残らないように）。

- **1-1 ＋ 3（kiroku_002）** 黒塗り復元パズルとして実装（2026-06-10）。
  - kiroku_001 に赤い黒塗りの現代語「████」（＝クラウド）の段落を追加。
    文脈手がかり：「書いたものは何でも████に置いておけた／消えない場所／
    どこからでも取り出せる場所」。脚注「赤い黒塗り語句は原文のまま。現代語と思われるが特定不能」。
  - kiroku_002 のキーワードを `['クラウド','クラウドストレージ']` に変更（月湯温泉の重複解消）。
  - kiroku_002 内の既存の赤い黒塗り（スマートフォンも、██████も）を ████ に統一
    （解いた後に「これも同じ語だ」と分かる反復）。
  - ヒント（Aスポーク）も「赤い黒塗りは、文脈から元の言葉を推定できます」に更新。
  - 検証：B（重複）・C（同時解放）解消。「クラウド」は可視テキストに出現しないことを確認。

### 手順4（シナリオ再構成）実装記録（2026-06-10　同日続き）

- **4-1** `archive_about` を「問いの提示」に書き換え。明かすのは「戸籍のない記録者／
  アーカイブを残した／封鎖されている／記録にない人が一人出てくる」まで。
  清治の名前・救出の経緯・「記録にない」の断定・「記録してください」の依頼を削除
  （依頼は choice が初出に）。導線キーワード（月湯温泉・脱線事故・M川事件・芙島市）は
  本文に残し、末尾に「最初の鍵は、もうこのページに書きました」のオンボーディングを追加。
- **4-2** `index.html` の追記から「これは私が読むためのものではない。あなたのものだ」を
  削除し、「誰に宛てたものなのかは、いまも分からない」で止めた（hidden の核心の予告を回避）。
- **4-4** 疑念→反転を実装。
  - `inochi`（第2層入口）：「目撃者の不在」を語り手自身に反転。「清治さんがいたという
    証拠は、たどればすべて私ひとりに行き着く」「あなたが疑うのなら、それは正しい疑い方です」。
  - `data_trace`（hidden 直前）：「私自身は何で証明されるのか」「記録の上では、ふたりとも
    いなかったことになっている」を追加（receiver_lock〜hidden の受信者反転への助走）。
  - `fumi_tegami` の「あなたが存在しないなんて、私には信じられません」が回答として機能
    （既存文・変更なし）。wiki 追記が「信じることを選ぶ」行為になる。
- **4-3** タコ（機械）の劣化＝時間圧。STORY_MSGS に損耗率の進行を追加
  （3%→14%→3█%→5█%。後半は文字化け混じり）。「閲覧された記録は固定されます。
  読まれていない記録から、先に壊れていきます」で「読むこと＝記録の救出」をゲーム内の
  手応えに変換。
- **4-5** 反復刈り込み（軽量）。「あなたはここにいた」は 1-4 対応で fumi_tegami がほぼ初出に。
  archive_about 書き換えで「名前はなかった」系の重複が2箇所減。telegram_003 の
  「清治さんは記録から消えた」再説明を1行に圧縮。bbs_003 の4連リフレインは
  ページ内の意図的な反復として維持。sns/momo の統合は未実施（保留）。

### メタ構造の実装（2026-06-10　同日続き・制作者発案）

設定：**プレイヤーは探索を始めた時点で「壁」を一枚透過してこちら側（架空の側）に来ている。**
「追記しない」のGoogle送還は、現実側への引き戻しのメタファー。
この設定は**一切言語化しない**。語彙は「壁のこちら側／そちら側」で統一し、
「世界線」という語は使わない（制作者指定）。

- `epilogue` 全面改修：
  - 追記しない＝**切断エンディング**。GAME CLEAR を出さない（「── 切断 ──／受信者は、
    登録されませんでした」「CONNECTION CLOSING」）。不明送信元の切断シーケンスを
    CSS遅延フェードで段階表示（受信者登録：拒否 → 固定解除 → 損耗再開 →
    「この記録は、壁のそちら側からは、見つかりません」→ 照合先：「松川事件」──実在の記録です
    → 転送）後、約10秒で Google検索「松川事件」へ同タブ送還。「考え直す」ボタンで中断可。
  - 追記した＝GAME CLEAR のあと、不明送信元の「補記」ボックスで現実への橋を一行
    （「壁のそちら側に、写し元となった実在の記録があります。名前は『松川事件』」）。
    新規タブの静かなリンクのみ、自動遷移なし。小蘭の声では語らせない。
- 入口側の暗示（最小限）：
  - `index.html` 起動フェード「起動しています…」→「接続しています…」
  - 導入オーバーレイに「接続元：照合できません」を1行追加
  - `wiki_skip` の「追記しない」ボタン説明を「接続を終了する。」に

### 追加提案 → 全6件実装済み（2026-06-10　サブエージェント実装・親検証）

1. **wiki_add：一行入力** ✓ — 架空wiki編集画面に「あなたの一行」textarea を追加。
   入力（空なら既定文「猫塚清治と蛸川小蘭は、ここにいた。」）を `koe_fumi_note` に保存し、
   epilogue の「追記済み」欄に「追記された一行：「…」」として表示（HTMLエスケープ済み・
   旧セーブ値 '1' は非表示）。
2. **深沢の「追記の追記」** ✓ — index.html がクリア後（koe_restored に epilogue）に
   後日談を表示。記録した→「通知は来なくなった。最後に礼のような短い信号」／
   しなかった→「端末は二度と起動しない。少しだけ軽くなった気がする」。
3. **セクタ固定率** ✓ — os.js に `getFixedPercent()`（閲覧済み/全ロックページ）。
   ブラウザのホームに「セクタ固定率：xx%　損耗進行中」を常時表示。読むほど上がる。
4. **sns の体験化** ✓ — 「声の速さと重さ」を不明送信元の挿入資料（現代SNS風モック）に
   全面リライト。速い声5件は表示後 opacity 0.25 まで減衰、遅い声（地元の年配者
   @fushima_obachan_1932・本人の記憶として整合済み）だけ金枠で残る。
   締め「速い声は、流れて消える。遅い声だけが、壁を透る。」
5. **bbs_003 名簿照合** ✓ — bbs_002 ビラの賛同者名簿（住所付き・「蛸川　██　住所不詳」
   ＋守る会の身元未確認注記＝既存の良い記述を保全）に小蘭の注記
   「＊塗り潰された名前を、私は知っている」を追加。bbs_003 のキーワードを
   `['蛸川小蘭','小蘭']` に変更（冤罪から変更）。bbs_003 冒頭に告白パート
   「記録を守ろうとする側の名簿からさえ、私は消える側だった」。
   archive_about の旧 keywords（蛸川小蘭/小蘭/蛸川）は衝突するため撤去（locked:false で不要だった）。
6. **受信条件の一行** ✓ — msg_receiver に「受信の条件は、ひとつだけ。記録が消えない側で、
   これを読めること。」を追加（「受信者は、あなたです」の直前）。

未実施（次の作業）：

- **3** 謎解き再設計の残り（photo_002 の画像観察化 / photo_003 / bbs_003 の名簿照合）
  ※ photo 系は画像内容（新聞の日付・駅名標の写り込み）が必要＝画像仕様書との調整が要る。
- 4-5 の残り：sns と momo の統合検討（第2層の論説調の連続を解消する場合）。
- 実ブラウザでの通し確認（この環境ではブラウザ自動検証不可）。特に：
  - kiroku_001 の黒塗りパズルが「クラウド」に辿り着けるか（テストプレイ推奨）
  - archive_about 再構成後も最初の検索（月湯温泉）に自然に辿り着けるか
  - 前提不足応答・ヒントトースト・損耗率メッセージの見え方
  - choice → wiki_add/wiki_skip → epilogue がボタンで正しく開くか
