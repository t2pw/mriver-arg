# 「声は壁を透して」制作引き継ぎメモ v14（端末移行版）
更新：2026年6月4日 / 対象ブランチ：`main`

このメモは、新しい開発端末で `git pull` 後に作業を再開するための最新引き継ぎです。
過去メモ v13 から、0604 D群のエンディング刷新と、受信者ロックの演出変更まで反映しています。

---

## 0. 最初に読むもの

1. `引き継ぎメモ_v14.md`（このファイル）
2. `設計書 確定版.md`
3. `アプリ基盤設計書_v1.md`
4. `画像仕様書_v1.md`（ただし `puzzle_recv.jpg` 方式は廃止メモ）
5. 必要時のみ `引き継ぎメモ_v13.md`

---

## 1. 現在の実装状況

- 静的HTML/JSサイト。ビルド不要。
- 入口は `index.html`、本体は `phone_shell.html`。
- ページ本文は `pages/*.js`、解析ツールは `tools/*.js`。
- ページやメッセージ本文を編集したら、必ず `node extract_scenario.js` で `scenario_text.txt` を再生成する。

今回の最新作業で、0604final の D群は実装済み。

- `pages/choice.js`
  - 「この手紙を、記録しますか」から、架空M川事件wikiへの追記依頼へ変更。
  - 小蘭は現代のWikipedia的な仕組みを知っている前提。
- `pages/wiki_add.js`
  - タイトルを「M川事件」に変更。
  - Wikipedia編集画面風レイアウトに変更。
  - 「編集を保存」ボタンで `localStorage 'koe_fumi_note' = '1'` を保存し `epilogue` へ遷移。
- `pages/wiki_skip.js`
  - 「記録しない」から「追記しない」へ文言変更。
- `pages/epilogue.js`
  - GAME CLEAR 演出追加。
  - 追記済みルート：小蘭からのお礼として全面刷新。
  - 未追記ルート：数秒後に Google 検索 `松川事件` へ遷移。
  - Xシェアボタン追加。
- `pages/momo.js`
  - 現在/過去対比を、実在の福島・松川・土湯・花見山・福島大学松川資料室の情報を架空化して再構成。

---

## 2. 重要なシナリオ変更：送信元は小蘭ではなく不明端末

以前はメッセージアプリに「蛸川小蘭」名義で私信が届く設計だったが、v14で変更した。

現行方針：

- メッセージ送信元は `繧ｿ◆縺薙％??` のような文字化けした不明送信元。
- 正体は最後まで明かさない。
- タコのぬいぐるみ内の機械/PDA/AIが送っていることを示唆するが、名前や説明は出さない。
- 小蘭本人がリアルタイムに送信しているような表現は避ける。
- 端末が「復元」「照合」「検知」して通知しているようなトーンにする。

変更済みファイル：

- `phone_shell.html`
  - 初回通知、`STORY_MSGS`、`THREADS` の送信元を不明送信元へ変更。
  - 本文も「小蘭からの私信」ではなく復元通知トーンへ変更。
- `pages/soran_intro.js`
  - 初期メッセージの送信者情報を不明送信元へ調整。
- `extract_scenario.js`
  - シナリオ出力見出しを「不明送信元からの復元通知」に変更。

---

## 3. 第3層ロック：`puzzle_recv.jpg` は廃止

画像で18個のモールス符号を読ませる方式は廃止。
理由：人間が読み取れる区切りを画像生成で安定させるのが難しく、プレイヤー体験として不安定。

現行仕様：

- `pages/receiver_lock.js` 画面内で、不明送信元からモールス列が届く。
- プレイヤーはモールス列を `tools/morse.js` に入力する。
- 結果：`412116261010224216`
- 2桁ずつ区切る：`41 21 16 26 10 10 22 42 16`
- `tools/hexconv.js` で変換：`アナタハココニイタ`
- ブラウザ検索欄に `アナタハココニイタ` または `あなたはここにいた` を入力すると `hidden` 解放。

`images/puzzle_recv.jpg` は現行実装では参照されない。作成・コミット不要。
`画像仕様書_v1.md` には旧仕様として残しているが、先頭に廃止注記を追加済み。

---

## 4. 解放ロジック・ツールの注意

- `os.js` のキーワード認証は完全一致。
- `hidden` のキーワード：
  - `あなたはここにいた`
  - `アナタハココニイタ`
- 新規ツールは作らない。
- 解析ツールは既存の2つのみ：
  - `tools/morse.js`
  - `tools/hexconv.js`
- `hexconv.js` には受信者ロック用の対応表が追加済み：
  - `10` → コ
  - `16` → タ
  - `21` → ナ
  - `22` → ニ
  - `26` → ハ
  - 既存 `41` → ア、`42` → イ など

---

## 5. 検証済み

実行済み：

```bash
node --check os.js
node --check pages/receiver_lock.js
node --check pages/loop.js
node --check pages/soran_intro.js
node --check pages/choice.js
node --check pages/wiki_add.js
node --check pages/wiki_skip.js
node --check pages/epilogue.js
node --check pages/momo.js
node extract_scenario.js
```

`scenario_text.txt` は再生成済み。

---

## 6. 残タスク

優先度高：

1. ブラウザで通し動作確認
   - `index.html` → `phone_shell.html`
   - メッセージ送信元が不明送信元に見えるか
   - `receiver_lock` のモールス列が読みやすいか
   - モールス → 数字 → 16進 → `アナタハココニイタ` で `hidden` が開くか
   - `choice → wiki_add/wiki_skip → epilogue` が自然か
   - 未追記ルートで Google 検索 `松川事件` へ遷移するか
   - Xシェアリンクが開くか

2. 文言最終確認
   - 不明送信元がタコ/PDAを示唆しつつ、正体を明かしすぎていないか
   - 小蘭本人からリアルタイムに届いているような文が残っていないか
   - `receiver_lock` の手順説明がプレイヤーに親切すぎる/不親切すぎる場合は調整

優先度中：

3. `引き継ぎメモ_v13.md` と `画像仕様書_v1.md` は v14 方針に合わせて一部更新済みだが、時間があれば文書全体を v14 に統一してもよい。

4. 未追跡のユーザーメモ `.txt` はコミットしない運用。
   - `シナリオ改善案_*.txt`
   - `テストプレイフィードバック*.txt`

---

## 7. 新端末での開始手順

```bash
git clone https://github.com/t2pw/mriver-arg.git
cd mriver-arg
```

その後、この順で確認する。

1. `CLAUDE.md`
2. `引き継ぎメモ_v14.md`
3. `設計書 確定版.md`
4. `アプリ基盤設計書_v1.md`
5. `画像仕様書_v1.md`

ビルドは不要。ブラウザで `index.html` を開けば確認できる。

