# 「声は壁を透して」制作引き継ぎメモ v15
更新：2026年6月5日 / 対象ブランチ：`main`

このメモは v14 以降、2026年6月5日の文言整理・地図整理・ツールUI修正を反映した最新引き継ぎです。
作業再開時はこのファイルを最初に読むこと。

---

## 0. 最初に読むもの

1. `引き継ぎメモ_v15.md`（このファイル）
2. `課題メモ_2026-06-05.md`
3. `設計書 確定版.md`
4. `アプリ基盤設計書_v1.md`
5. `画像仕様書_v1.md`（ただし `puzzle_recv.jpg` 方式は廃止）
6. 必要時のみ `引き継ぎメモ_v14.md`

---

## 1. 実装状態

- 静的HTML/JSサイト。ビルド不要。
- 入口は `index.html`、本体は `phone_shell.html`。
- ページ本文は `pages/*.js`、解析ツールは `tools/*.js`。
- ページやメッセージ本文を編集したら、必ず `node extract_scenario.js` で `scenario_text.txt` を再生成する。
- 現在の作業ツリーには未コミット変更がある。

主な未コミット変更ファイル：

- `extract_scenario.js`
- `os.js`
- `pages/choice.js`
- `pages/epilogue.js`
- `pages/fumi_tegami.js`
- `pages/hub_002.js`
- `pages/inochi.js`
- `pages/map_001.js`
- `pages/map_002.js`
- `pages/map_003.js`
- `pages/soran_intro.js`
- `pages/wiki_add.js`
- `scenario_text.txt`
- `tools/morse.js`
- `課題メモ_2026-06-05.md`

---

## 2. 今日の変更点

### 2.1 終盤：ふみ設定の削除

設定変更により、猫塚ふみは現在の終盤には存在しない。役割は蛸川小蘭へ統合済み。

可視文言から `ふみ` / `猫塚ふみ` は削除済み。

変更済み：

- `pages/choice.js`
  - 「ふみさんの手紙」→「小蘭の手紙」
  - 「ふみさんが書いたこと」→「小蘭が書いたこと」
- `pages/wiki_add.js`
  - 未収録資料の差出人を猫塚ふみから蛸川小蘭へ変更。
  - 「清治とふみの痕跡」→「清治の痕跡」へ変更。
- `pages/epilogue.js`
  - 「ふみさんの手紙」→「小蘭の手紙」
  - 記録対象を「猫塚清治、蛸川小蘭の手紙とアーカイブ」に変更。

注意：

- 内部IDの `fumi_tegami` と localStorage キー `koe_fumi_note` はまだ残っている。
- これは表示されない実装名なので、今回は未変更。
- 将来リネームするなら `os.js`、`pages/hidden.js`、`phone_shell.html`、`extract_scenario.js` などの参照もまとめて変更すること。

### 2.2 終盤CTAの文言整理

- `pages/fumi_tegami.js`
  - 「この手紙を、記録しますか」→「この手紙があったことを、追記してください」
  - 「架空文集『声は壁を透して』未収録欄」→「架空M川事件wiki　未収録資料」
- `pages/choice.js`
  - コメント上の「小蘭からの依頼」感が強い表現を、アーカイブ本文上の依頼として整理。

### 2.3 地図：五つのポイント・座標文言を削除

座標を読ませる仕様ではなくなったため、意味深なだけの「五つのポイント」「座標」表現を削除。

変更済み：

- `pages/soran_intro.js`
  - 「五つのポイントが、それぞれ別の記録を指しています」→「地名の一部が欠落しています」
- `pages/map_001.js`
  - タイトルを「芙島市　欠落した地名」に変更。
  - 可視の緯度経度表示を削除。
  - 「座標」「ポイント」ではなく「地名」「印」に変更。
- `pages/map_002.js`
  - 「金色のポイント」→「金色の印」
- `pages/map_003.js`
  - 「青いポイント」→「青い印」
  - 「座標のみ」→削除。
- `pages/hub_002.js`
  - 「地図の座標を辿った」→「地図の記録を辿った」
- `os.js`
  - コメント上の旧マーカー/座標前提を更新。

注意：

- Leaflet 実装上の `lat/lng` は残っている。これは地図表示の内部座標であり、プレイヤーに読ませる要素ではない。

### 2.4 `inochi.js` の意味不明文言修正

- `pages/inochi.js`
  - 「記録から消えたことで、『04』の一部になった」
  - →「記録から消えたことで、目撃者不在という空白の中に押し込まれた」

### 2.5 モールス読取機の結果枠はみ出し修正

- `tools/morse.js`
  - 長い数字列が変換結果枠からはみ出す問題を修正。
  - `overflow-wrap:anywhere`、`word-break:break-all`、`white-space:normal` を追加。
  - `letter-spacing` を `.25em` から `.12em` に縮小。

### 2.6 `extract_scenario.js` の整形改善

- `scenario_text.txt` 生成時の余分な空行を圧縮する処理を追加。
- コメントを「小蘭からのメッセージ」から「不明送信元からの復元通知」へ変更。
- この変更により `scenario_text.txt` の diff は大きく見えるが、多くは空行整理。

---

## 3. 現行の重要仕様

### 3.1 メッセージ送信元

- メッセージ送信元は小蘭ではなく、文字化けした不明送信元。
- 正体は最後まで明かさない。
- タコのぬいぐるみ内の機械/PDA/AIが送っていることは示唆してよいが、説明しすぎない。
- 小蘭本人がリアルタイム送信しているような表現は避ける。

### 3.2 第3層ロック

- `puzzle_recv.jpg` は廃止。作成・コミット不要。
- `pages/receiver_lock.js` 画面内で不明送信元からモールス列が届く。
- 解答手順：
  1. モールス読取機 → `412116261010224216`
  2. 2桁区切り → `41 21 16 26 10 10 22 42 16`
  3. 16進変換器 → `アナタハココニイタ`
  4. ブラウザ検索欄に `アナタハココニイタ` または `あなたはここにいた`
  5. `hidden` 解放

### 3.3 キーワード認証

- `os.js` のキーワード認証は完全一致。
- `hidden` のキーワード：
  - `あなたはここにいた`
  - `アナタハココニイタ`
- 新規ツールは作らない。解析ツールは既存の2つのみ：
  - `tools/morse.js`
  - `tools/hexconv.js`

---

## 4. 検証済み

実行済み：

```bash
node --check os.js
Get-ChildItem pages -Filter *.js | ForEach-Object { node --check $_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }
Get-ChildItem tools -Filter *.js | ForEach-Object { node --check $_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }
node --check extract_scenario.js
node extract_scenario.js
```

確認済み：

- `rg "ふみ|猫塚ふみ"` で、可視実装・生成シナリオに該当なし。
- `rg` で旧地図表現（`五つのポイント`、`座標データ`、`座標のみ`、`赤いポイント` 等）に該当なし。
- モールス列 → `412116261010224216` の変換確認済み。
- `アナタハココニイタ` / `あなたはここにいた` で `hidden` 解放ロジック確認済み。

未実施：

- 実ブラウザでの通し確認。
- この環境ではブラウザ本体/Playwright が PATH 上になく、自動ブラウザ検証は未実施。

---

## 5. 残タスク

`課題メモ_2026-06-05.md` にまとめ済み。優先的に確認すること：

- 沼の中に遊園地がある問題。
- タコの端子がLANケーブルに見える／LANケーブルである問題。
- 蒼沼マップのフレーバーとして座標が多すぎる問題。
- 「福島市」の単語が先に発見されると進みすぎて難しくなる問題。
- 押し入れの中にいる必要がない問題。
  - 小蘭は月湯で働いて24年を過ごした設定。
- 「問いた」→「問うた」。
- 「北に行け」とは言っていない問題。
- 「月湯温泉」のキーワード重複。
- 「封筒」「書けない手紙」のキーワード設定を再考。
- アドレスバーのURLを毎回消すのがだるいかもしれない問題。
- 出せない手紙と言っていたのに出している問題（`bbs_001`）。

---

## 6. 次回の推奨作業順

1. `git status --short` で未コミット差分を確認。
2. `課題メモ_2026-06-05.md` を読む。
3. まずテキスト整合性の問題から直す。
   - 押し入れ設定
   - 北に行け
   - 問いた/問うた
   - bbs_001 の「出せない手紙」矛盾
4. 次に解放難易度の問題を直す。
   - 福島市の実在語フック
   - 月湯温泉キーワード重複
   - 封筒/書けない手紙キーワード
   - アドレスバー入力UX
5. 最後に地図・画像・端子などビジュアル/設定の整合性を調整。
6. 変更後は必ず `node extract_scenario.js` を実行。

---

## 7. 端末での確認

ビルド不要。ブラウザで `index.html` を開けば確認できる。

特に次回ブラウザで確認すること：

- メッセージ送信元が不明送信元に見えるか。
- 地図ページに旧「座標」「五つのポイント」感が残っていないか。
- モールス読取機の結果が枠内に収まるか。
- `choice → wiki_add/wiki_skip → epilogue` に「ふみ」が残っていないか。
- `receiver_lock` から `hidden` 解放まで自然に進めるか。
