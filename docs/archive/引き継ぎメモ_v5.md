# 「声は壁を透して」制作引き継ぎメモ v5
更新：2026年6月（claude.ai → Claude Code 移行時）

---

## このメモの使い方

Claude Code のプロジェクトに以下を配置してから作業を開始してください：
1. このメモ（引き継ぎメモ_v5.md）
2. 設計書（設計書_確定版.md）
3. アプリ基盤設計書_v1.md
4. 画像仕様書_v1.md
5. GitHub からpull済みの本番ファイル一式

---

## 現在の到達点

| 課題 | 状態 |
|---|---|
| ①シナリオ改善 | **完了**（v3時点） |
| ②Eスポーク試作 | **完了**（v4時点） |
| ②Eスポーク本番統合 | **完了**（v5このチャット） |
| ②Dスポーク地図実地名問題 | **完了**（v5このチャット） |
| ②A・B・Cスポーク | **完了**（謎解きなし・既存コンテンツそのまま） |
| ③キーワード実在語フック | **未着手** |
| ④デバッグ | **部分完了**（DEVパネルにツール強制導入/削除を追加済み） |
| 既知バグ：松川駅廃駅描写 | **未対応** |
| 画像：puzzle_e_hex.jpg / puzzle_e_shoji.jpg | **未生成** |

---

## v5で確定・変更したファイル一覧

### 差し替え必須（新版をGitHubにpush済みのはず）

| ファイル | 変更内容 |
|---|---|
| `os.js` | v3.1：freesoftをPAGESに登録、uninstallApp追加 |
| `phone_shell.html` | 解析アプリ基盤を統合（tool-grid / installTool / openTool / TOOL_UI / setSearchAndFocus / DEVパネル拡張） |
| `pages/map_001.js` | v3：imageOverlay化＋座標を map_fushima.jpg に合わせて調整 |
| `pages/map_002.js` | v3：同上 |
| `pages/map_003.js` | v3：同上 |
| `pages/telegram_001.js` | v5：ツール入手導線を撤去、画像＋キーワードタグのみ（telegram_001_v5.jsとして生成済み） |

### 既存のまま使うファイル（変更なし）

- `pages/freesoft.js` ── Shell.installTool を呼ぶ作りで本番Shellと結線済み
- `tools/hexconv.js` ── TOOL_UI['hexconv'] を定義、本番Shellと結線済み
- `tools/morse.js` ── TOOL_UI['morse'] を定義、本番Shellと結線済み
- `pages/kiroku_001〜004.js` ── A スポーク、謎解きなし、そのまま
- `pages/photo_001〜003.js` ── B スポーク、謎解きなし、そのまま（松川駅バグは未対応）
- `pages/bbs_001〜003.js` ── C スポーク、謎解きなし、そのまま
- その他すべての pages/*.js ── 変更なし

### 新規追加が必要な画像ファイル

| ファイル | 状態 | 備考 |
|---|---|---|
| `images/map_fushima.jpg` | **生成済み** | 古い手描き地図風、1448×1086px。Geminiで生成。 |
| `images/puzzle_e_hex.jpg` | **未生成** | Eスポーク謎解き第1段（16進）。仕様は画像仕様書_v1.md参照。 |
| `images/puzzle_e_shoji.jpg` | **未生成** | Eスポーク謎解き第2段（モールス・障子の穿孔）。同上。 |

---

## os.js v3.1 の主な変更点

### 1. freesoft をPAGESに登録（A案・構造的順序保証）

```js
{ id:'freesoft', title:"T.Watanabe's Tools Page", locked:true,
  keywords:['バイナリ','モールス','16進','穿孔','解析ツール','フリーソフト'],
  prereqs:['telegram_001'], phase:1, spokeGroup:null, icon:'💾' },
```

- `prereqs:['telegram_001']` により「暗号ページを見てから検索で配布サイト到達」の順序を構造的に保証
- `spokeGroup:null` でスポーク進捗%には影響しない
- telegram_001未復元の状態でキーワード検索しても freesoft は解放されない（検証済み）

### 2. uninstallApp を追加

```js
const uninstallApp = id => {
  const s = getInstalled();
  if (!s.has(id)) return false;
  s.delete(id); ls.saveSet(INSTALLED_KEY, s); return true;
};
```

DEVパネルからのツール強制削除用。

---

## phone_shell.html の主な変更点

### 追加した機能

1. **`#tool-grid`**（ホーム画面、`#grid`の下）
   - インストール済み解析アプリを動的描画
   - 0件の場合は CSS `:empty` で非表示

2. **`renderToolGrid()`**
   - `KoeOS.getInstalled()` を読んでアイコンを生成
   - `closeApp()` 時にも呼んでホームを最新状態に更新

3. **`installTool(id)`**
   - インストールモーダル表示（プログレスバー約2秒）
   - 完了後: `KoeOS.installApp(id)` → `goHome()` → `renderToolGrid()` → `flashNewIcon(id)` → pushNotif

4. **`openTool(id)`**
   - `TOOL_UI[id]()` を呼んで解析アプリ画面を表示
   - curApp / curTool を分離管理（ブラウザと解析アプリが干渉しない）

5. **`setSearchAndFocus(kw)`**
   - ブラウザを開き、検索欄にキーワードをセットしてフォーカス
   - `telegram_001.js` のキーワードタグのonclick から呼ばれる

6. **`TOOL_UI = {}`**（定数定義）
   - `PAGE_CONTENT` / `MSG_THREADS` と並列で定義
   - `tools/*.js` の動的ロードで各ツールが `TOOL_UI['hexconv']` 等に代入

7. **`loadToolScript(src)`**
   - `loadPageScript` と同様の動的ロード関数
   - `data-tool` 属性で重複ロード防止

8. **`loadInstalledTools()`**
   - 起動時に `KoeOS.getInstalled()` を読んで既インストール分を復元ロード

9. **DEVパネル拡張**
   - TOOL APPS セクションを追加
   - 各ツールに「導入」「削除」ボタン（`_devToggleTool(id)` を呼ぶ）

### インストールモーダルのHTML

`#screen` 内の末尾に追加：

```html
<div id="install-modal">
  <div id="install-card">
    <div id="install-icon">💾</div>
    <div id="install-name">—</div>
    <div id="install-status">ツールを取得しています…</div>
    <div id="install-bar-track"><div id="install-bar-fill"></div></div>
  </div>
</div>
```

---

## map_001〜003.js v3 の変更内容

### 変更の背景

旧版は OpenStreetMap タイルを使用しており、実在する福島市の地名・道路が表示されていた。架空都市「芙島市」の世界観と矛盾するため、カスタム画像に差し替え。

### 変更内容

- `L.tileLayer()` を廃止
- `L.imageOverlay('images/map_fushima.jpg', IMG_BOUNDS)` に差し替え
- `attributionControl: false`（帰属表示なし）
- `maxBounds: IMG_BOUNDS` + `maxBoundsViscosity: 1.0`（画像範囲外にスクロール不可）

### 座標系（map_fushima.jpg に合わせて逆算）

画像サイズ 1448×1086px から各スポットのピクセル位置を目視計測し、以下の四隅座標で線形変換：

```
北端(上): lat = 37.83
南端(下): lat = 37.67
西端(左): lng = 140.38
東端(右): lng = 140.60

IMG_BOUNDS = [[37.67, 140.38], [37.83, 140.60]]  ← 全3ファイル共通
```

| スポット | lat | lng | 画像上の位置 |
|---|---|---|---|
| 芙島市中心 | 37.7578 | 140.4453 | 左寄り中央 |
| 偲山 | 37.7504 | 140.4408 | 市街地内南側 |
| 蒼沼 | 37.7946 | 140.5395 | 右上の湖 |
| 桃見山 | 37.7224 | 140.5365 | 右下の山 |
| M川駅 | 37.6989 | 140.4833 | 下部中央 |

マーカー位置が画像とずれる場合は、地図上クリック時の座標取得機能を追加して微調整できる。

---

## 残課題の詳細

### 課題③ キーワード実在語フック（未着手）

os.js の各ページ定義の `keywords` 配列に実在語を追加するだけ。
`submitKeyword` は部分一致なので配列に追加するだけでヒットする。

追加候補（引き継ぎメモ v3 より）：
- `kiroku_001`：「福島」「昭和24年」など
- `map_002`：「花見山」
- `momo`：「花見山」「福島市」
- 他スポークも同様に設計書の実在語フック案を参照

### 既知バグ：松川駅の廃駅描写（未対応）

`photo_002.js` と `momo.js` に「M川駅跡」「廃線のまま」という描写がある。
実在の松川駅は稼働中であるため、架空設定として明示するか、描写を変更するかの判断が必要。
対応方針は未確定。

### 画像生成（未着手）

`puzzle_e_hex.jpg` / `puzzle_e_shoji.jpg` の生成プロンプトは「Gemini画像プロンプト_E.md」（v4時点で作成済みのはず）を参照。
なければ画像仕様書_v1.md から再生成可能。

---

## 技術メモ（v4からの追記）

- **`tools/*.js` のロード**: `loadToolScript()` で動的ロード。`data-tool` 属性で重複防止。`loadPageScript` と別管理なのは `PAGE_CONTENT` と `TOOL_UI` が別オブジェクトのため。
- **`curApp` / `curTool` の分離**: `openTool()` 呼び出し時は `curApp=null, curTool=id` にセット。`closeApp()` で両方リセット。`closeMsgView()` の再描画判定（`curApp==='messages'`）が正しく機能するよう維持。
- **`maxBounds` と `imageOverlay` の組み合わせ**: `maxBoundsViscosity:1.0` で画像外への移動を完全にブロック。ズームレベルは `minZoom:11, maxZoom:13` で画像の解像度に合わせて調整。
- **マーカーの `border` 色**: 旧版は `#e2e0da`（明るい色）だったが、地図画像の背景がセピアなので `#1a1a1f`（暗い色）に変更してマーカーが目立つようにした。
- **v4からの継続技術メモ**（`innerHTML`注入後の結線は `setTimeout(0)`、PAGE_CONTENT定義順、bNavigateは本番のものを使う等）は引き継ぎメモ v4 参照。
