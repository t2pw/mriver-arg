# 謎解き画像 仕様書 v1 ── 「声は壁を透して」課題②

このドキュメントは、謎解きで使う「小蘭が残した生データ」画像の仕様。
プレイヤーはこの画像を**目で読み取り**、ホーム画面の解析アプリに**手入力**して変換する。
画像は読み取れる必要があるが、答えそのものを書いてはいけない（変換が必要な状態で置く）。

---

## 共通方針

- **配置場所**：`images/` フォルダ（既存の photo_*.jpg と同じ）。
- **トーン**：セピア〜モノクロ、古い記録・スキャン風。既存アーカイブの世界観（1949〜1963年の遺物を2026年にスキャンした体）に合わせる。
- **答えは画像に書かない**：例えば「0314」という数字を直接書かず、モールスや穴の配置として埋め込む。プレイヤーが解析アプリで変換して初めて数字になる。
- **読み取り可能性**：スマホ画面（横幅390px想定）で拡大しなくても各記号が判別できる解像度・コントラストにする。
- **推奨サイズ**：横幅1200px前後（Retina表示でぼけない）。アスペクト比は各画像の指定に従う。
- **ファイル形式**：写真調はJPG、線画・図版で透過が要るならPNG。

---

## スポークE（電文）で必要な画像

Eスポークは「16進変換器」と「モールス読取機」の2アプリを使う2段構成。
画像も2枚必要。

### 画像1：`puzzle_e_hex.jpg`（第1段・16進）

- **内容**：小蘭の電文ヘッダ。`53 55 4A` の3つの16進2桁が、電文用紙／パンチカード／帳面いずれかの体裁で記録されている。
- **見せ方の例**：古い電報用紙に、タイプ打ちまたは手書きで「受信者識別子（部分）」として `53 55 4A` が並ぶ。各値の脇に小さく通し番号①②③があると、読む順が伝わりやすい。
- **答えに直結させない**：`53→ショ` 等の対応表は画像に載せず、**解析アプリ側**に持たせる。画像には16進値だけ。
- **アスペクト比**：横長（16:9〜2:1程度）。
- **読み取らせる値**：`53` `55` `4A`（この順）。

### 画像2：`puzzle_e_shoji.jpg`（第2段・モールス）

- **内容**：月湯温泉の宿の障子。針穴（虫食いに見せかけた穴）が**4つの升（パネル）**に分かれて開いている。各升が1文字＝モールス1桁。
- **穴の規則**：
  - 小さい丸い穴 ＝ 短点（・）
  - 横長の裂け目／長い穴 ＝ 長点（−）
  - 1つの升の中で、穴が**左から右**に並ぶ。
  - 升は**左上→右上→左下→右下**の順（または明確に番号を振る）に読む。
- **各升の穴パターン**（モールス数字）：
  | 升 | 穴の並び（左→右） | モールス | 数字 |
  |---|---|---|---|
  | 第1升 | 長 長 長 長 長 | `−−−−−` | 0 |
  | 第2升 | 長 長 長 短 短 | `−−−・・` | 8 |
  | 第3升 | 短 長 長 長 長 | `・−−−−` | 1 |
  | 第4升 | 長 短 短 短 短 | `−・・・・` | 6 |
- **答えに直結させない**：モールス→数字の対応表は**解析アプリ側**に持たせる。画像は穴の物理配置だけ。
- **アスペクト比**：縦長または正方形（障子らしく）。
- **読み取らせる結果**：`0` `8` `1` `6` → 最終キーワード `0816`。
- **演出のヒント**：障子全体を撮った写真の上に、穴の位置がうっすら光って見えるレタッチがあると「小蘭が意図的に開けた」感が出る（必須ではない）。

---

## 解析アプリ側に持たせるもの（画像には載せない）

混同しないための整理。以下は**アプリのUIに組み込む**ので画像不要：

- 16進→カタカナ対応表
- モールス→数字対応表
- 入力欄・変換ボタン・結果表示

画像に載せるのは「生データ（16進値そのもの／穴の配置そのもの）」だけ。

---

## 今後のスポーク用（A〜D）予備メモ

試作Eが固まってから本設計するが、画像が要りそうなものを先出し：

- **B写真**：既存の photo_koaru / photo_mkawa 等に、読み取り対象（裏面メモの数字、看板の文字）を埋めた版が要るかも。
- **C掲示板**：帳面の罫線ズレ＝バイナリ画像（`puzzle_c_binary.jpg`）。
- **D地図**：座標は既存Leafletで足りる可能性が高く、画像は不要かも。
- **A手記**：テキスト主体なので画像不要の見込み。

---

## チェックリスト（画像納品時）

- [ ] 答えそのもの（0314等）が画像に書かれていない
- [ ] スマホ幅で各記号が判別できる
- [ ] 読む順序が画像から分かる（番号 or 自然な並び）
- [ ] ファイル名が仕様通り
- [ ] セピア／スキャン風で世界観に合っている
