# エージェントで回す手順（Claude Code／Codex等）

1. シナリオディレクトリでエージェントを起動する。
2. 最初に送る：「`keeper_prompt_template.md`（［ ］埋め済み）とシナリオ文書と現在の確定状態を読んで、DLとして開始してください」
3. 以降はPL発言だけ。エージェントは描写のたびに確定状態ファイルを更新する。
4. 中断時：「ここまでを確定状態に保存して」。再開時：同じディレクトリで起動し、3ファイルを再読込させる。
5. 重くなったら：「ログを要約して確定状態に畳んで」。
6. 終幕後、`eval_sheet.md` に記録する。
