# 「声は壁を透して」制作引き継ぎメモ v10
更新：2026年6月4日（Claude Code セッション v10 / テストプレイ後・改善計画確定）

---

## このメモの使い方

**作業前に必ず以下を読むこと：**
1. このメモ（引き継ぎメモ_v10.md）← 最重要
2. 設計書 確定版.md
3. アプリ基盤設計書_v1.md
4. 画像仕様書_v1.md

**重要：os.js の keywords 配列は Claude が自由に編集可能。制限なし。**

---

## プロジェクト概要

ARG（代替現実ゲーム）型 Web フィクション。
GitHub Pages で公開。HTML/CSS/JavaScript の静的サイト。
スマートフォン画面を模した SPA（`phone_shell.html`）が本体。

**コアコンセプト：** 「記録されなかった手紙を、あなたが記録する」

---

## v10セッションで完了した作業

### 名称変更・黒塗り
- **十湯温泉 → 月湯温泉（つきゆおんせん）** 全ファイル置換完了
- **広瀬和郎 → ██████（黒塗り）** 全ファイル（姓のみ「広瀬」も含む）完了
- 設計書の読み仮名も「つきゆおんせん」に更新済み

### バグ修正
- **3層アクセス不能バグ修正**（最重要）
  - `checkPrereqUnlocks()` が新規解放IDを返すよう変更 → `_loadPage` でスクリプトを自動ロード
  - `_checkHub()` が `'hub_002'` を返すよう変更 → `submitKeyword` で hub_002.js も自動ロード
  - `msg_inside` に hub_002 へのナビゲーションボタン追加
  - `data_trace` → `hidden` → `fumi_tegami` → `choice` の「次へ」ボタン追加
- **archive_aboutボタン修正**：タップ時にブラウザを開き該当記事に直接遷移するよう修正

### UX改善（D群・完了）
- **D-1** URLバータップで全選択（そのまま打ち替え可能）
- **D-2** 「復元」ボタン・プレースホルダー → 「検索」に変更
- **D-3** freesoft.jsを2013年Google Playストア風レイアウトに全面再設計

### キーワード同期
- 引き継ぎメモ_v9 のキーワード表を os.js 実態に合わせて全面同期済み

---

## テストプレイフィードバック（0604）による改善計画

フィードバック全文：`テストプレイフィードバック0604.txt`

### 優先度C：キーワード変更（次のステップ）← 次回着手

os.js の PAGES 配列のみ変更。コンテンツ書き直しはB群で別途対応。

| ページ | 現在のキーワード | 変更後 | 備考 |
|---|---|---|---|
| kiroku_002 | 偲山 | 月湯温泉 | kiroku_001の出典ヒントコメントも更新 |
| bbs_002 | 署名運動、署名 | M川事件を守る会 | |
| bbs_003 | 一千三百、一千三百名 | 冤罪、自白強要、冤罪事件 | |
| map_001 | 座標、座標データ | 芙島市 | |
| map_002 | N-0816、記録番号 | 桃見山、花見山 | |
| photo_001 | 記録されなかった人物 | 脱線、脱線事故 | prereqsの見直し検討 |
| photo_002 | 撮影者不詳 | 昭和24年、昭和二十四年 | |
| photo_003 | 暖簾 | M川駅、松川駅 | |
| inochi | 冤罪、自白、証拠隠蔽 | なぜ冤罪は生まれたか | hub_002に誘導テキスト追加も必要 |

**第2層（inochi〜data_trace）追加仕様：**
キーワード解放廃止 → ページ読了時に自動解放（prereqsのみで解放）に変更。
これに伴い、各ページのキーワードを空にする（`keywords:[]`）。

**koaru_record 削除：**
フィードバックによりページ削除。inochi が第2層の入口になる。
- `hub_002.js` の最後の誘導テキストを「存在しない人物」→「なぜ冤罪は生まれたか」に変更
- `os.js` の koaru_record エントリ削除、inochi の prereqs を `['hub_002']` に変更

### 優先度A：構造・設計変更（合意してから着手）

| # | 内容 |
|---|---|
| A-1 | archive_about + soran_profile を統合。小蘭の独白記事として再執筆。タコ画像添付。消息を絶つ直前に書いた演出。 |
| A-2 | koaru_record 削除（C群で先行実施） |
| A-3 | 第2層（inochi〜data_trace）をキーワード解放廃止・読了自動解放に変更（C群で先行実施） |
| A-4 | 「誰のスマホか」導入設定の追加 |

### 優先度B：コンテンツ書き直し（第一人称化）

| # | 対象 | 内容 |
|---|---|---|
| B-1 | kiroku_001 | 清治との会話演出追加。追われる経緯を説明。キーワードはA-1完成後に確定。 |
| B-2 | kiroku_002 | 月湯に行かざるを得なかった理由を説明。 |
| B-3 | kiroku_003 | 小蘭の感情描写を増やす（命の恩人への思い）。 |
| B-4 | photo_001 | 「小蘭が清治を調べて入手した写真」として書き直し。 |
| B-5 | photo_003 | 「M川駅近くに清治の家」描写追加。右下文字を「M川事件」に変更。AI画像再生成必要。 |
| B-6 | bbs_003 | 「猫塚清治は今日も存在しない」→「清治さんの名前はそこになかった」 |
| B-7 | map_002・map_003・telegram_001〜003 | 小蘭視点に書き直し。map_003 から「N-0816」削除。 |
| B-8 | inochi〜data_trace | 全ページ小蘭視点に統一。loop 不要箇所削除。data_trace はタコのデータ保持理由の説明に再構成。 |
| B-9 | fumi_tegami 内の「十湯から芙島市に出て」 | 月湯に変更済みだが文章の整合性確認 |

---

## 確定した物語設定（v8以降不変）

### 主要人物

| 人物 | 設定 |
|---|---|
| **蛸川小蘭**（たこがわ こらん） | 現代のITエンジニア（30代女性）。M川事件の調査中に1949年8月16日の夜に引き寄せられた。戸籍なし。偽名「蛸川」で月湯温泉に24年潜伏。14年間M川事件と清治の消息を記録し続けた。1963年9月12日以降消息不明。 |
| **猫塚清治**（ねこづか せいじ） | K鉄の保線作業員、34歳。事件の夜に現場にいた。小蘭を当局から匿い逃がした。被告名簿にも証人名簿にも記録されず消えた「存在しない人物」。 |

### 暗号キー・設定
- **N-0816** = 8月16日 = M川事件が起きた夜の日付
- 月湯温泉（つきゆおんせん）= 土湯温泉をモチーフとした架空の温泉地
- 広瀬和郎 = 架空の作家（全て黒塗り ██████ に変更済み）

### エンディング構造
- **記録する** → wiki_add → epilogue（猫塚清治と蛸川小蘭がアーカイブに確定）
- **記録しない** → wiki_skip → epilogue（ふたりは消えたまま）

---

## 全ページ一覧とキーワード（os.js 実態 / v10時点）

### 自由ページ（キーワード不要）
| id | タイトル |
|---|---|
| archive_about | このアーカイブについて |
| soran_profile | 蛸川小蘭とは誰か |

### スポークA 手記（prereq チェーン）
| id | キーワード | 出典ヒント |
|---|---|---|
| kiroku_001 | 月湯温泉、土湯温泉 | soran_profile「月湯温泉に逃げ込み」 |
| kiroku_002 | 偲山 ※C群で月湯温泉に変更予定 | kiroku_001冒頭「低い山がひとつ──偲山」 |
| kiroku_003 | 猫塚清治、清治 | kiroku_002末尾「次に書くべきは、彼のこと」 |
| kiroku_004 | 封筒、書けない手紙 | kiroku_003末尾「封筒に宛名を書けない」 |

### スポークB 写真
| id | キーワード | 出典ヒント |
|---|---|---|
| photo_001 | 記録されなかった人物 ※C群で変更予定 | soran_profile |
| photo_002 | 撮影者不詳 ※C群で変更予定 | photo_001末尾 |
| photo_003 | 暖簾 ※C群で変更予定 | photo_002末尾 |

### スポークC 掲示板
| id | キーワード | 出典ヒント |
|---|---|---|
| bbs_001 | M川事件、松川事件 | soran_profile |
| bbs_002 | 署名運動、署名 ※C群で変更予定 | bbs_001 |
| bbs_003 | 一千三百、一千三百名 ※C群で変更予定 | bbs_002 |

### スポークD 地図（謎解き）
| id | キーワード | 出典ヒント |
|---|---|---|
| map_001 | 座標、座標データ ※C群で変更予定 | soran_intro |
| map_002 | N-0816、記録番号 ※C群で変更予定 | map_001 異常マーカーpopup |
| map_003 | 蒼沼ブルーランド、蒼沼 | map_002 1963年3月マーカー |

### スポークE 電文（謎解き）
| id | キーワード | 出典ヒント |
|---|---|---|
| telegram_001 | 復号キー、暗号キー、復号 | soran_intro 添付 |
| freesoft | バイナリ、モールス、16進、穿孔、解析ツール、フリーソフト | telegram_001 kwTag |
| telegram_002 | 0816、N-0816 | hex＋モールス謎解き答え |
| telegram_003 | 声は壁を透して、文集、未収録 | telegram_002末尾 |

### 第2層（hub_002 解放後）
| id | キーワード | 備考 |
|---|---|---|
| koaru_record | 存在しない人物 | ※C群で削除予定 |
| inochi | 冤罪、自白、証拠隠蔽 | ※C群で「なぜ冤罪は生まれたか」に変更・第2層入口に |
| voices | 四面楚歌、広津和郎 | ※C群でキーワード廃止・prereq自動解放に |
| tegami | 三百通 | ※C群でキーワード廃止・prereq自動解放に |
| sns | 二グラム、2グラム | ※C群でキーワード廃止・prereq自動解放に |
| momo | 帰れなかった | ※C群でキーワード廃止・prereq自動解放に |
| loop | 未来の誰か、記録の行方 | ※C群でキーワード廃止・prereq自動解放に |
| data_trace | 存在の異常 | ※C群でキーワード廃止・prereq自動解放に |

### 第3層（prereq チェーン、キーワード不要）
data_trace → hidden → fumi_tegami → choice → wiki_add / wiki_skip → epilogue

---

## アーキテクチャ概要

```
index.html          ← phone_shell.html へのリダイレクト
phone_shell.html    ← メインSPA（全UI・ナビ・ゲームロジック含む）
os.js               ← PAGES 定義・キーワード認証・進捗管理
pages/*.js          ← 各ページコンテンツ（PAGE_CONTENT[id] = () => HTML）
tools/*.js          ← 解析アプリUI（TOOL_UI[id] = () => HTML）
images/             ← 写真・謎解き画像
```

### ページ解放システム
1. `os.js` の `PAGES` 配列にキーワードと prereqs を定義
2. プレイヤーがブラウザの検索欄にキーワードを入力
3. `KoeOS.submitKeyword()` で部分一致照合（双方向）
4. prereqs が全て復元済みなら解放
5. **第2層ハブ（hub_002）は全スポーク完了で自動解放**
6. **`keywords:[]` のページは prereqs 充足で自動解放（v10修正済み）**
   - `checkPrereqUnlocks()` が新規解放ID配列を返す → `_loadPage` がスクリプトを自動ロード

### 解析アプリシステム
- freesoft.js ページからインストール（2013年Google Playストア風UI）
- hexconv.js：16進数 → カタカナ変換（53 55 4A → 「ショウジ」）
- morse.js：モールス符号 → 数字変換（0〜9）

---

## 謎解きフロー（Eスポーク詳細）

```
telegram_001 を開く
  ├─ puzzle_e_hex.jpg を見る
  │   → hexconv ツールで「53 55 4A」を変換 → 「ショウジ」
  │   → ヒント：障子の穿孔図を見よ
  └─ puzzle_e_shoji.jpg を見る
      → morse ツールで4升のモールスを変換 → 「0816」
      → ブラウザに「0816」を入力 → telegram_002 解放
```

**puzzle_e_shoji.jpg**：現在 0314 パターン。**0816 パターンへ差し替えが必要（ユーザー手動）。**

---

## 残課題まとめ

### 🔴 次回着手（C群キーワード変更）

1. koaru_record エントリを os.js から削除
2. inochi の prereqs を `['koaru_record']` → `['hub_002']` に変更、キーワードを `['なぜ冤罪は生まれたか']` に変更
3. hub_002.js の第2層誘導テキストを「存在しない人物」→「なぜ冤罪は生まれたか」に変更
4. 第2層（voices〜data_trace）の keywords を全て `[]` に変更（prereq自動解放）
5. 各スポークのキーワード変更（上記テーブル参照）

### 🟡 その後（A群・構造変更）

- archive_about + soran_profile 統合・再執筆（小蘭独白形式）
- 「誰のスマホか」導入設定の追加

### 🟢 最後（B群・コンテンツ書き直し）

- 全ページ小蘭視点に統一
- 各ページの詳細改善（上記B群テーブル参照）

### 🔵 ユーザー手動作業

- puzzle_e_shoji.jpg の再生成（0816パターン）
  - 参考：引き継ぎメモ_v8.md の「puzzle_e_shoji.jpg 再生成プロンプト」

---

## 技術メモ・注意事項

### ページ追加・変更時のルール
1. `os.js` の PAGES 配列に id・title・keywords・prereqs・phase を追加
2. `pages/{id}.js` を作成し `PAGE_CONTENT['{id}'] = () => \`...\`` で定義
3. `phone_shell.html` の ALWAYS_LOAD には変更不要（既存3件のみ）

### 重要な実装メモ
- **innerHTML 注入後の結線**：`setTimeout(()=>{...}, 0)` を使う
- **キーワード部分一致**：`norm.includes(nk) || nk.includes(norm)`（双方向）
- **prereq自動解放**：`checkPrereqUnlocks()` が返す配列で `loadPageScript` を呼ぶ（v10修正済み）
- **`soran_intro` はメッセージスレッド**：`MSG_THREADS['soran_intro']` で定義（PAGE_CONTENT ではない）
- **Leaflet 地図**：`ensureLeaflet()` をローカルに定義（map_001〜003 各ファイルに内蔵）

### やってはいけないこと
- `os.js` を編集する際、SPOKES 配列・localStorage キー・API 構造は変更しない
- 画像の `onerror` フォールバックに答えとなる数値・文字を含めない
- `wiki_add.js` の `localStorage.setItem('koe_fumi_note', '1')` キー名を変えると epilogue が壊れる

---

## ファイル構成（v10 現在）

```
index.html
phone_shell.html              ← メインSPA（v10: URLバー自動選択・検索ボタン修正済み）
os.js                         ← PAGES 定義（v10: checkPrereqUnlocks 戻り値修正済み）
scenario_text.txt             ← 最新シナリオテキスト
テストプレイフィードバック0604.txt ← v10改善計画の元データ
引き継ぎメモ_v10.md           ← このファイル
pages/
  archive_about.js  soran_profile.js  soran_intro.js
  kiroku_001.js〜004.js
  photo_001.js〜003.js
  bbs_001.js〜003.js
  map_001.js〜003.js
  telegram_001.js〜003.js
  freesoft.js                 ← v10: 2013年Google Playストア風に再設計
  hub_002.js
  koaru_record.js             ← C群で削除予定
  inochi.js  voices.js  tegami.js
  sns.js  momo.js  loop.js
  data_trace.js               ← v10: hidden へのナビゲーションボタン追加済み
  hidden.js                   ← v10: fumi_tegami へのナビゲーションボタン追加済み
  fumi_tegami.js              ← v10: choice へのナビゲーションボタン追加済み
  choice.js  wiki_add.js  wiki_skip.js  epilogue.js
tools/
  hexconv.js  morse.js
images/
  （省略 / 画像仕様書_v1.md 参照）
  puzzle_e_shoji.jpg  ★要差し替え（0816パターン）
```
